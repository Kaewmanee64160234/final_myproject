plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    //  buildFeatures {
    //     compose = true
    // }

    // composeOptions {
    //     // https://developer.android.com/jetpack/androidx/releases/compose-kotlin
    //     kotlinCompilerExtensionVersion = "1.4.8"
    // }

    namespace = "com.example.identity_scan"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "com.example.identity_scan"
        // You can update the following values to match your application needs.
        // For more information, see: https://flutter.dev/to/review-gradle-config.
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
        }
    }

    //  dependencies {
    //     // Use the Compose BOM (Bill of Materials) to manage version compatibility
    //     def composeBom = platform("androidx.compose:compose-bom:2024.10.01")  // Make sure this version is compatible with your Kotlin version
    //     implementation(composeBom)
    //     androidTestImplementation(composeBom)

    //     // Add Jetpack Compose libraries
    //     implementation("androidx.compose.material3:material3")
    //     implementation("androidx.compose.foundation:foundation")
    //     implementation("androidx.compose.ui:ui")
    //     implementation("androidx.compose.material:material") // Optional, choose if using Material Design 2

    //     // Android Studio Preview support for Compose
    //     implementation("androidx.compose.ui:ui-tooling-preview")
    //     debugImplementation("androidx.compose.ui:ui-tooling")

    //     // UI Tests
    //     androidTestImplementation("androidx.compose.ui:ui-test-junit4")
    //     debugImplementation("androidx.compose.ui:ui-test-manifest")

    //     // Optional libraries for Compose
    //     implementation("androidx.compose.material:material-icons-core")
    //     implementation("androidx.compose.material:material-icons-extended")
    //     implementation("androidx.compose.material3.adaptive:adaptive")

    //     // Optional - Activity Compose Integration
    //     implementation("androidx.activity:activity-compose:1.9.2")

    //     // Optional - ViewModel Compose Integration
    //     implementation("androidx.lifecycle:lifecycle-viewmodel-compose:2.8.5")

    //     // Optional - LiveData Integration
    //     implementation("androidx.compose.runtime:runtime-livedata")

    //     // Optional - RxJava Integration
    //     implementation("androidx.compose.runtime:runtime-rxjava2")
    // }

}

flutter {
    source = "../.."
}

dependencies {
    implementation 'androidx.constraintlayout:constraintlayout:2.2.0'
}
